# ТЗ: Ребрендинг «VoiceSystem» — лендинг (KY/RU) + рестайлинг ЛК
<!-- v2: акцент изменён с синего на глубокий чёрный (монохром) -->

> **Исполнитель:** Claude Code (отдельная сессия).
> **Ветка:** создать новую от `1208-v1-registr` (например `1308-rebrand-voicesystem`).
> **Тип работ:** ТОЛЬКО внешний вид и тексты. Логика, API, схемы данных — неприкосновенны.

---

## 0. Контекст проекта

Это клон SaaS-платформы голосовых ИИ-ассистентов (оригинал — Voicyfy), разворачиваемый
для партнёра в Кыргызстане под брендом **VoiceSystem**.

- Лендинг: React + Vite, исходники в `frontend/src/`, собирается в `backend/static/landing/`.
- ЛК (личный кабинет): ~20 самостоятельных vanilla HTML/CSS/JS страниц в `backend/static/`.
- Бэкенд: FastAPI, отдаёт всё как статику. **Бэкенд в этом ТЗ не трогаем вообще.**

### ⚠️ Правило сборки лендинга (из CLAUDE.md, обязательно!)

Render собирает только Python. После ЛЮБОЙ правки в `frontend/src/**`:

```bash
cd frontend && npm ci && npm run build
cd .. && git add -A backend/static/landing frontend
```

В коммите обязаны быть: удалённый старый `index-<hash>.js`, новый `index-<hash>.js`,
обновлённый `index.html`. Если в `git status` нет изменений в `backend/static/landing/` —
сборка не выполнена, прод останется старым.

---

## 1. Жёсткие ограничения (проверять перед каждым коммитом)

**НЕЛЬЗЯ:**
1. Менять/переименовывать/удалять `id`, `data-*` атрибуты и классы, на которые ссылается
   JavaScript (обработчики в `<script>` секциях страниц ЛК, `backend/static/agents/index.js`,
   `backend/static/js/*`). Перед изменением класса — `grep` по его использованию в JS.
2. Трогать файлы: `backend/api/**`, `backend/services/**`, `backend/models/**`,
   `backend/core/**`, `app.py`, `main.py`, `alembic/**`, `render.yaml`, `requirements.txt`.
3. Менять `frontend/src/utils/api.js` (кроме текстов ошибок — их не трогать тоже),
   `frontend/src/hooks/**` — это логика.
4. Менять фавикон и файлы логотипов (`favicon.ico`, `android-chrome-*.png`,
   `manifest.json`, картинки логотипа) — остаются как есть, заменим позже.
5. Менять юридические страницы (`public-offer.html`, `terms-of-service.html`,
   `privacy-policy.html`, `payment-terms.html`) — отдельная задача с юристом.
6. Менять шаблоны email в `backend/services/email_service.py` — отдельная задача.
7. Ломать существующую вёрстку мобильной версии.

**МОЖНО:** HTML-разметку (аккуратно), CSS полностью, видимые текстовые строки,
добавлять новые CSS-файлы и словари переводов, добавлять чисто-презентационный JS
(анимации, переключатель языка) без обращения к API.

---

## 2. Брендинг

| Параметр | Значение |
|---|---|
| Название | **VoiceSystem** (в тексте: «VoiceSystem», кириллицей не писать) |
| Где заменить «Voicyfy» | Все видимые строки лендинга и ЛК: тайтлы `<title>`, шапки, сайдбары, футеры, тексты секций, `aria-label` |
| Где НЕ заменять | Код/переменные/классы (`voicyfy-link` и т.п. — оставить), email-шаблоны, юр-страницы, фавикон/логотипы |
| Слоган RU | «Голосовой ИИ для вашего бизнеса. Говорит. Слушает. Понимает.» |
| Слоган KY | «Бизнесиңиз үчүн үн жасалма интеллекти. Сүйлөйт. Угат. Түшүнөт.» |

Проверка после замены: `grep -rn "Voicyfy" frontend/src backend/static --include="*.html" -l`
— допустимые остатки только в юр-страницах, email-доках и js-виджетах (их не трогаем).

---

## 3. Дизайн-система «White Depth Grid / Black Accent»

Белая тема с ощущением глубины и инженерной сетки, акцент — **глубокий чёрный**
(монохромный, «премиальный» стиль: чёрные кнопки и активные состояния на белом,
цвет используется только для статусов и одного редкого тёплого штриха). Все токены — в
`frontend/src/styles/variables.css` (лендинг) и в новом общем файле
`backend/static/css/voicesystem-theme.css` (ЛК).

### 3.1 Палитра

```css
:root {
  /* Поверхности — белый с层ами глубины */
  --vs-bg: #FAFBFD;             /* фон страницы, чуть холодный */
  --vs-surface: #FFFFFF;        /* карточки */
  --vs-surface-raised: #FFFFFF; /* карточки над карточками (отличаются тенью) */
  --vs-ink: #0E1526;            /* основной текст, почти чёрный с синевой */
  --vs-ink-soft: #4A5568;       /* вторичный текст */
  --vs-ink-faint: #8A94A6;      /* подписи, плейсхолдеры */

  /* Акцент — глубокий чёрный (монохром) */
  --vs-accent: #0A0C10;         /* кнопки, активные состояния, ссылки-underline */
  --vs-accent-hover: #1C2028;   /* hover чёрных элементов — чуть светлее, не синее */
  --vs-accent-soft: #F1F3F6;    /* заливки чипов, hover-фоны, активный пункт сайдбара */
  --vs-on-accent: #FFFFFF;      /* текст/иконки на чёрном */
  --vs-warm: #F59E0B;           /* единственный цветной штрих: бейджи «рекомендуем», редкие подчёркивания */

  --vs-success: #16A34A;
  --vs-error: #DC2626;

  /* Сетка и линии */
  --vs-line: #E6E8EC;
  --vs-grid-line: rgba(10, 12, 16, 0.05);
}
```

Правила монохрома: чёрный — единственный акцент. Кнопка-CTA: чёрная с белым
текстом, hover — `--vs-accent-hover` + подъём. Вторичная кнопка: белая с чёрной
рамкой 1px, hover — инверсия (чёрный фон, белый текст, transition 200ms — очень
эффектно). Ссылки: чёрные с подчёркиванием, hover — подчёркивание утолщается.
Не вводить других цветов, кроме `--vs-warm` (точечно) и статусных success/error.

### 3.2 Глубина (elevation) — 4 уровня теней

Тени мягкие, двухслойные (ключевая + ambient), никакого чистого чёрного:

```css
--vs-shadow-1: 0 1px 2px rgba(10,12,16,.05), 0 1px 3px rgba(10,12,16,.06);
--vs-shadow-2: 0 4px 12px rgba(10,12,16,.07), 0 1px 3px rgba(10,12,16,.05);
--vs-shadow-3: 0 12px 32px rgba(10,12,16,.12), 0 2px 8px rgba(10,12,16,.06);
--vs-shadow-4: 0 24px 64px rgba(10,12,16,.18), 0 4px 16px rgba(10,12,16,.08);
```

Правила: страница `shadow-0`, карточки `shadow-1`, hover-карточки `shadow-2→3`,
модалки/дропдауны `shadow-4`. Глубину дополняют границы `1px solid var(--vs-line)`.

### 3.3 Фоновая сетка

Инженерная сетка на фоне ключевых секций (hero, тарифы) и body ЛК:

```css
.vs-grid-bg {
  background-image:
    linear-gradient(var(--vs-grid-line) 1px, transparent 1px),
    linear-gradient(90deg, var(--vs-grid-line) 1px, transparent 1px);
  background-size: 32px 32px;
}
/* + радиальная маска, чтобы сетка растворялась к краям: */
.vs-grid-fade { -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 40%, #000 40%, transparent 100%); }
```

Поверх сетки в hero — 2–3 больших размытых пятна (`filter: blur(120px)`) для
«дыхания». В монохроме чёрные пятна выглядят грязью, поэтому: одно тёплое
(`--vs-warm`, opacity .10) и одно-два графитовых (`#0A0C10`, opacity .04–.06) —
едва заметные, дающие объём, а не цвет.

### 3.4 Типографика

- Шрифт: **Inter** (или Manrope) — обязательно проверить кыргызские глифы
  **Ң ң, Ө ө, Ү ү** (Inter поддерживает). Подключать через `@font-face` с
  self-hosted woff2 или существующий механизм проекта — НЕ добавлять внешние CDN,
  если страницы уже работают офлайн-шрифтами.
- Шкала: 56/40/28/20/16/14 px, заголовки `letter-spacing: -0.02em`, `font-weight: 700`.
- Радиусы: 10px (кнопки, инпуты), 16px (карточки), 24px (hero-панели).

---

## 4. Лендинг: структура работ

Файлы секций (реальные): `HeroSection.jsx`, `ProvidersSection.jsx`, `UseCasesSection.jsx`,
`ShowcaseSection.jsx`, `AgentSection.jsx`, `CodeSection.jsx`, `PricingSection.jsx`,
`PhoneCTASection.jsx`, `Navbar.jsx`, `Footer.jsx`, `AuthModal.jsx`, `AuthSection/*`,
плюс декоративные `MeshBackground.jsx`, `SphereAnimation.jsx`, `ScrollProgress.jsx`.
Стили: `frontend/src/styles/*.css`.

### 4.1 Двуязычность KY/RU (без бэкенда!)

**Архитектура:**

1. Создать `frontend/src/i18n/ky.js` и `frontend/src/i18n/ru.js` — плоские словари
   (`{ "nav.pricing": "Тарифтер", ... }`), и `frontend/src/i18n/index.jsx` —
   React-контекст `LangProvider` + хук `useT()`:
   - язык по умолчанию — **`ky`**;
   - выбор хранится в `localStorage.setItem('vs_lang', ...)`;
   - при смене — `document.documentElement.lang = 'ky'|'ru'`;
   - никаких запросов на сервер.
2. Все видимые строки во всех компонентах лендинга вынести в словари. Хардкода
   строк в JSX остаться не должно (проверка: поиск кириллицы в `*.jsx`).
3. Переключатель в `Navbar.jsx`: капсула `KY | RU` (текстовые ярлыки, без флагов),
   активный язык — заливка `--vs-accent-soft`; дублировать в мобильном меню и в `Footer`.
4. `AuthModal`/`AuthSection` тоже переводятся. **Сообщения об ошибках, приходящие
   с сервера, не трогать** — показывать как есть.

**Ключевые переводы (утверждены, использовать именно так):**

| Ключ | RU | KY |
|---|---|---|
| nav.features | Возможности | Мүмкүнчүлүктөр |
| nav.pricing | Тарифы | Тарифтер |
| nav.contacts | Контакты | Байланыш |
| nav.login | Вход | Кирүү |
| nav.signup | Регистрация | Катталуу |
| hero.title | Голосовой ИИ для вашего бизнеса | Бизнесиңиз үчүн үн жасалма интеллекти |
| hero.subtitle | Говорит. Слушает. Понимает. | Сүйлөйт. Угат. Түшүнөт. |
| hero.cta | Начать бесплатно | Акысыз баштоо |
| auth.startFree | Начните бесплатно | Акысыз баштаңыз |
| auth.trial3days | 3 дня полного доступа без ограничений | 3 күн чектөөсүз толук мүмкүнчүлүк |
| auth.name | Имя | Атыңыз |
| auth.password | Пароль | Сырсөз |
| auth.passwordHint | Минимум 8 символов, буквы и цифры | Кеминде 8 белги, тамгалар жана сандар |
| auth.company | Компания (опционально) | Компания (милдеттүү эмес) |
| auth.register | Зарегистрироваться | Катталуу |
| auth.haveAccount | Уже есть аккаунт? Войти | Аккаунтуңуз барбы? Кирүү |
| auth.codeSent | Код отправлен! Проверьте email. | Код жөнөтүлдү! Email каттыгыңызды текшериңиз. |
| common.loading | Загрузка… | Жүктөлүүдө… |

Остальные секции переводить самостоятельно, тон: уважительная форма («сиз»),
деловой, без канцелярита. Числа/цены не локализуются. Если сомнение в термине —
оставить русское слово в скобках при первом употреблении.

### 4.2 Редизайн секций (белый + глубина + сетка)

- **Navbar:** белый, `backdrop-filter: blur(12px)`, снизу линия `--vs-line`;
  при скролле > 8px появляется `--vs-shadow-1`. Название «VoiceSystem».
- **Hero:** фон `--vs-bg` + `.vs-grid-bg.vs-grid-fade` + blur-пятна. Заголовок
  из словаря, под ним строка-«бейдж» с тёплым акцентом. Существующую
  `SphereAnimation` сохранить, но перекрасить в чёрно-белую (графитовую) гамму —
  чёрная сфера/линии на белом смотрятся очень выразительно.
- **Карточные секции (UseCases, Providers, Showcase, Pricing):** карточки
  `--vs-surface`, граница + `--vs-shadow-1`, hover: `translateY(-4px)` +
  `--vs-shadow-3` (см. §6). Тариф «рекомендуем» — приподнят (`shadow-3`),
  тонкая рамка `--vs-accent`, бейдж на `--vs-warm`.
- **CodeSection:** окно кода — светлая тема (фон `#F6F8FC`), рамка, заголовок-«окно»
  с тремя точками. ⚠️ Демо-виджет грузится с `https://voicyfy.ru` — блок скрыть
  за пропсом/флагом `SHOW_WIDGET_DEMO = false` (не удалять код), т.к. виджет чужого прода.
- **Footer:** белый, колонки, вместо `info@voicyfy.ru` — не менять на новый email
  (пока нет), просто убрать строку mailto или оставить без изменений, если убрать
  нельзя без правки логики.

---

## 5. ЛК: рестайлинг сайдбара и общего вида

### 5.1 Файлы

Страницы с сайдбаром (полный список): `dashboard.html`, `agents.html`,
`gemini-agents.html`, `grok-agents.html`, `cartesia-agents.html`, `fish-agents.html`,
`yandex-agents.html`, `cascade.html`, `elevenlabs-agents.html`, `knowledge-base.html`,
`conversations.html`, `crm.html`, `crm-contact.html` (если есть сайдбар), `settings.html`,
`telephony.html`, `outbound-calls.html`, `integrations.html`, `admin.html`,
`api-docs.html`, `prompts-wiki.html`. Файлы `*_old.html` — НЕ трогать.

### 5.2 Что сделать

1. Создать **один общий файл** `backend/static/css/voicesystem-theme.css` с токенами
   из §3 и стилями сайдбара/карточек/кнопок. Подключить `<link>` в каждую страницу
   ПОСЛЕДНИМ (перекрывает встроенные стили). Инлайн-стили страниц не переписывать
   массово — перекрывать теми же селекторами из темы (меньше диффа — меньше риска).
2. Сайдбар: фон `--vs-surface`, справа граница `--vs-line`, лёгкая сетка-фон
   (`.vs-grid-bg` с opacity 0.5). Активный пункт — заливка `--vs-accent-soft`,
   слева вертикальная полоска 3px `--vs-accent`, скруглённая. Hover — плавный
   сдвиг фона + иконка `translateX(2px)`.
3. Название в шапке сайдбара: «Voicyfy» → «VoiceSystem» (только текст! логотип-картинку
   не менять). `<title>` каждой страницы: «… — VoiceSystem».
4. Кнопки/инпуты/карточки в контенте — привести к токенам через тему-оверрайд
   (цвет акцента, радиусы, тени). Таблицы: строки при hover — фон `--vs-accent-soft`
   50% прозрачности.
5. **Язык ЛК не трогаем** — остаётся русский (двуязычность ЛК — отдельный этап).
6. Ничего не менять в `<script>` секциях, кроме случаев, где JS вставляет
   видимую строку «Voicyfy» (менять только строковый литерал).

---

## 6. Анимации — «душа и живость»

### 6.1 Технические правила (обязательны)

- Анимировать только `transform` и `opacity` (60fps, без reflow). Никаких
  анимаций `width/height/top/left/box-shadow` в keyframes (тень менять можно
  через transition на hover — это редкое событие).
- Все анимации обёрнуты в `@media (prefers-reduced-motion: no-preference)`.
  При `reduce` — всё статично, контент виден сразу.
- Изинги: появление `cubic-bezier(0.16, 1, 0.3, 1)` (easeOutExpo-подобный),
  микро-hover `cubic-bezier(0.34, 1.56, 0.64, 1)` (лёгкий overshoot).
- Длительности: hover 150–200ms, появления 500–700ms, декоративные циклы 6–20s.
- Scroll-появления — через `IntersectionObserver` (порог 0.15, `once: true`),
  утилита-класс `.vs-reveal` + `.is-visible`. На лендинге сделать маленький
  компонент/хук `useReveal()`; в ЛК — 15 строк vanilla JS в теме (отдельный
  `voicesystem-motion.js`, подключается `defer`).

### 6.2 Каталог анимаций — лендинг

| Где | Что |
|---|---|
| Hero: заголовок | Появление каскадом: заголовок → подзаголовок → CTA, каждый `opacity 0→1` + `translateY(16px→0)`, задержки 0/120/240ms |
| Hero: blur-пятна | Медленный дрейф `translate/scale` циклом 18–24s, `will-change: transform` |
| Hero: сетка | Лёгкий параллакс: `background-position` сдвигается на 10–20px от scroll (через `transform` контейнера, не bg-position, если дешевле) |
| Navbar | Появление тени при скролле (transition 200ms); подчёркивание пунктов — линия растёт из центра (`scaleX 0→1`) |
| Переключатель KY/RU | «Пилюля» активного языка перемещается `transform` слайдом 200ms |
| Карточки (все секции) | Reveal по одному с шагом 80ms; hover: `translateY(-4px)` + тень `1→3` + иконка внутри `scale(1.06)` |
| Кнопка CTA | Hover: `translateY(-2px)` + тень; active: `scale(0.98)`; по клику — короткий ripple (псевдоэлемент, `scale 0→2.5` + fade 450ms) |
| Цифры/метрики (если есть) | Count-up от 0 при появлении в вьюпорте (rAF, 800ms, easeOut) |
| PricingSection | Рекомендованный тариф: бейдж мягко «дышит» `scale 1↔1.04` циклом 3s |
| Формы Auth | Ошибка — shake `translateX ±6px` 300ms; успех — галочка рисуется `stroke-dashoffset` 400ms; фокус инпута — рамка `--vs-accent` + мягкое свечение `box-shadow: 0 0 0 4px var(--vs-accent-soft)` |
| InlineNotification | Вход `translateY(-8px→0)+opacity`, выход — reverse |
| ScrollProgress | Оставить, перекрасить в `--vs-accent` |
| Footer | Ссылки: hover — `translateX(3px)` со стрелкой-псевдоэлементом |

### 6.3 Каталог анимаций — ЛК (сдержаннее, это рабочий инструмент)

| Где | Что |
|---|---|
| Сайдбар: пункты | Hover: фон-заливка 150ms + иконка `translateX(2px)`; активная полоска — `scaleY 0→1` 200ms при переключении |
| Карточки/статы дашборда | Reveal при загрузке страницы каскадом (шаг 60ms, только первый экран) |
| Кнопки | Как на лендинге (hover-lift + active-press), без ripple |
| Таблицы | Hover строки — фон 120ms; новая строка — fade-in |
| Модалки | Открытие: overlay fade 200ms + окно `scale(0.96→1)+opacity` 250ms |
| Дропдауны | `translateY(-6px→0)+opacity` 180ms |
| Тосты/уведомления | Слайд справа `translateX(16px→0)` |
| Спиннеры загрузки | Заменить цвет на `--vs-accent`; для карточек — skeleton-shimmer (градиентная полоса, цикл 1.6s) — только CSS, без изменения JS-логики загрузки |

---

## 7. Порядок выполнения (рекомендуемый)

1. Ветка от `1208-v1-registr`.
2. Дизайн-токены: `variables.css` (лендинг) + `voicesystem-theme.css` (ЛК).
3. i18n-каркас лендинга (`frontend/src/i18n/*`), перевод всех строк, переключатель.
4. Рестайл лендинга посекционно (Navbar → Hero → карточные → Auth → Footer).
5. Анимации лендинга (`useReveal`, ripple, каскады).
6. ЛК: подключить тему во все страницы из §5.1, сайдбар, замена «Voicyfy»→«VoiceSystem».
7. ЛК: `voicesystem-motion.js` (reveal + модалки уже анимируются CSS-ом).
8. `npm run build`, проверить diff `backend/static/landing/`, закоммитить всё.
9. Прогнать чек-лист §8. Коммитить логическими порциями (токены / i18n / лендинг /
   ЛК / анимации), в каждом коммите — рабочее состояние.

---

## 8. Чек-лист приёмки

**Функциональность (ничего не сломано):**
- [ ] Регистрация → письмо → ввод кода → дашборд: работает как до правок.
- [ ] Логин/логаут: работает, логаут ведёт на `/` (НЕ на voicyfy.ru).
- [ ] Все страницы ЛК открываются без JS-ошибок в консоли (проверить каждую из §5.1).
- [ ] `git diff` не содержит изменений в `backend/api|services|models|core`, `app.py`.

**Ребрендинг:**
- [ ] На лендинге и в ЛК нигде не видно «Voicyfy» (глазами + grep по видимым строкам).
- [ ] `<title>` всех страниц — с «VoiceSystem».

**Языки:**
- [ ] Лендинг по умолчанию открывается на кыргызском.
- [ ] Переключатель KY/RU работает, выбор переживает перезагрузку (localStorage).
- [ ] Глифы Ң ө Ү отображаются выбранным шрифтом (не fallback-ом) — проверить визуально.
- [ ] `document.documentElement.lang` меняется.

**Дизайн:**
- [ ] Белая тема: нет тёмных «островов» из старой темы ни на одной странице ЛК.
- [ ] Сетка видна в hero и фоне ЛК, растворяется к краям, не рябит на retina.
- [ ] Тени соответствуют шкале §3.2 (модалка глубже карточки, карточка глубже фона).
- [ ] Мобильная версия лендинга (360px): ничего не разъехалось, переключатель языка доступен.

**Анимации:**
- [ ] Reveal-каскады срабатывают один раз, при `prefers-reduced-motion: reduce` — отключены.
- [ ] Нет дёрганий/лагов при скролле (проверить Performance-панелью, long tasks < 50ms).
- [ ] Hover-состояния есть у всех интерактивных элементов.

**Сборка:**
- [ ] В коммите с правками `frontend/src/**` есть изменения `backend/static/landing/`.
- [ ] `npm run build` проходит без ошибок и warning'ов о missing imports.

---

## 9. Вне скоупа (НЕ делать в этой задаче)

- Фавикон, логотипы, `manifest.json` — остаются старые.
- Email-шаблоны (`email_service.py`) — бренд в письмах меняется отдельной задачей.
- Юридические страницы (оферта, условия, приватность) — отдельно, с юристом.
- Двуязычность страниц ЛК — отдельный этап (сейчас ЛК остаётся русским).
- Домен, `HOST_URL`, любые env — инфраструктурная задача.
- Тёмная тема — не делаем (проект — единая белая тема).
